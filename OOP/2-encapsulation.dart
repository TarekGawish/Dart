import '2.1-praviteAttribute.dart';

void main(){


  People tarek = People("Tarek", true);

  
  tarek.age = 33;
  print(tarek.age);
}

//first step make the attribute that you want to control -->  private

// make it in another file

// set the value of private attribute by set method

// and get the value by get method

//note: private attribute can not use directly in the object